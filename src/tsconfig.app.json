{
    "extends": "../tsconfig.json",
    "resolveJsonModule" : "true",
    "compilerOptions": {
        "outDir": "../out-tsc/app",
        "types": [
            "node"
        ]
    },
    "exclude": [
        "../tests/karma/test.ts",
        "**/*.spec.ts",
        "**/*.worker.ts"
    ]
}
